# first define the fun 
# theparameters( _, _) in the def must be variables like (a, b, x, y1, etc)
def fun_name(a,b):
    # using return 
    return a+b
# now fun is saved and values are given in variables
sum = fun_name(5,2)
# now printing the fum by using print statement
print(sum)
print(fun_name(10,80))

# position is taken in order of the variables and values 