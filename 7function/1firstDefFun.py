# first define the fun 
# theparameters( _, _) in the def must be variables like (a, b, x, y1, etc)
def fun_name(a,b):
    print(a+b)
# calling fun by using values that ar placed in variables
fun_name(2,5)